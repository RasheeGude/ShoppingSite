function logout() {
  // Replace this with your logout logic on the server-side (e.g., clearing session/token)
  // For example, if using server-side session, you may call an API endpoint to clear the session.

  // After successful logout, redirect the user to the login page
  window.location.href = "login.html";
}
